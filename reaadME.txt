para enmpezar son dos programas difernetes, app.py hace correr survey, ticket, fuera, conectandose a fresh

el de google es un prograama aparte para detectar cambios en la panilla de horarios, cuando los detecta, actualiza el json que va a usar app, 
para saber quien esta operativo, y los mensajes predeterminados para que se puedan cambiar facilmente, junto con la encuesta

programas  pip install Flask gspread google-auth requests pytz


python -m venv .venv   // crear entono virtual
source .venv/bin/activate    // activar
 pip install Flask gspread google-auth requests   // instalar dependencias
 asiganr entorno virtual
 Abre tu proyecto/carpeta en VS Code (la carpeta API google donde tienes tus archivos).

Abre la Paleta de Comandos:

Presiona Ctrl+Shift+P (en Windows/Linux) o Cmd+Shift+P (en macOS).
Escribe y selecciona Python: Select Interpreter:

Comienza a escribir "Python: Select Interpreter" y selecciónalo de la lista que aparece.
Elige el intérprete de tu entorno virtual:

VS Code te mostrará una lista de los intérpretes de Python que ha detectado. Deberías ver una opción que apunte al Python dentro de tu carpeta .venv. Usualmente la ruta será algo como:
./.venv/bin/python
Python 3.13.x ('.venv': venv)
O la ruta completa a .../API google/.venv/bin/python
Selecciona esta opción.
Espera un momento y Recarga la Ventana (si es necesario):